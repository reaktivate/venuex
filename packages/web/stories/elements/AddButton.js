import React from 'react';
import { storiesOf } from '@storybook/react';
import AddButton from 'ui/elements/AddButton.js';

storiesOf('elements/AddButton', module).add('Default', () => <AddButton />);
