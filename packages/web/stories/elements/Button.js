import React from 'react';
import { storiesOf } from '@storybook/react';
import Button from '../../ui/elements/Button';

storiesOf('elements/Button', module).add('default', () => <Button label={'Button'} />);
